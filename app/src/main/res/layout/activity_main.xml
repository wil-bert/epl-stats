<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="com.moringaschool.eplstats.ui.MainActivity">

    <ImageView
        android:id="@+id/image"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:src="@drawable/epl" />

    <TextView
        android:id="@+id/appNameTextView"
        android:layout_width="387dp"
        android:layout_height="199dp"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="38dp"
        android:layout_marginEnd="-17dp"
        android:fontFamily="@font/lemonada_bold"
        android:text="LEAGUE STATS"
        android:textColor="@color/primary_text"
        android:textSize="40dp" />

    <Button
        android:id="@+id/findTeamButton"
        android:layout_above="@+id/savedCompetitionsButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:background="@color/primary_text"
        android:text="Find Team"
        android:textColor="@color/primary_light" />

    <Button
        android:id="@+id/findLoginButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="63dp"
        android:background="@color/primary_text"
        android:text="Login"
        android:textColor="@color/primary_light" />

    <Button
        android:id="@+id/savedCompetitionsButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_alignParentBottom="true"
        android:layout_marginStart="0dp"
        android:layout_marginBottom="153dp"
        android:background="@color/primary_text"
        android:text="Saved Competition"
        android:textColor="@color/primary_light" />


</RelativeLayout>